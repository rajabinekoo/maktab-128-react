import { CustomersWrapper } from "../organisms/customer";

export const CustomersPage: React.FC = () => {
  return (
    <main className="w-screen min-h-[calc(100vh-60px)]">
      <CustomersWrapper />
    </main>
  );
};
